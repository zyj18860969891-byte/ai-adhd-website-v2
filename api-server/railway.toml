# API Server Railway Configuration
[build]
builder = "dockerfile"
dockerfilePath = "api-server/Dockerfile"

[env]
NODE_ENV = "production"
PORT = "3003"

[deploy]
numReplicas = 1
sleepApplication = false