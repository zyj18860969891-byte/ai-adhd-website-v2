# ChurnFlow MCP Railway Configuration
[build]
builder = "nixpacks"

[env]
NODE_ENV = "production"
PORT = "3008"

[run]
startCommand = "npm run build && npm start"

[deploy]
startCommand = "npm run build && npm start"