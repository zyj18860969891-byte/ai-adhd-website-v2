# ChurnFlow MCP Railway Configuration
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[env]
NODE_ENV = "production"
PORT = "3008"

[run]
startCommand = "npm start"

[deploy]
startCommand = "npm start"