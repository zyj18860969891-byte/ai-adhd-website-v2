# ChurnFlow MCP Railway Configuration
[build]
builder = "nixpacks"

[env]
NODE_ENV = "production"
PORT = "3008"

[run]
startCommand = "npm install typescript && npm run build && npm start"

[deploy]
startCommand = "npm install typescript && npm run build && npm start"