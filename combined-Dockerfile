# Combined Dockerfile for API Server and MCP Services
FROM node:lts-alpine

# Install necessary build tools and bash
RUN apk add --no-cache bash

# Create app directory
WORKDIR /app

# Install app dependencies
COPY package*.json ./

# Install dependencies without running lifecycle scripts
RUN npm install --ignore-scripts

# Bundle app source code
COPY . .

# Fix permissions for node_modules binaries
RUN chmod +x node_modules/.bin/*

# Make the startup script executable (explicitly check in /app)
RUN if [ -f /app/start-all-services.sh ]; then chmod +x /app/start-all-services.sh; else echo "Checking current directory:"; pwd; ls -la; echo "Looking for start-all-services.sh:"; find . -name "start-all-services.sh" 2>/dev/null; exit 1; fi

# Build the TypeScript code for each service separately
RUN cd api-server && npm run build
RUN cd churnflow-mcp && npm run build
RUN cd mcp-shrimp-task-manager && npm run build

# Expose ports for services
EXPOSE 3003 3008 3009

# Command to run all services in the container
CMD [ "./start-all-services.sh" ]